{% extends 'base.html.twig' %}

{% block title %}Contact
{% endblock %}

{% block body %}
<div class="row p-5">
	<div class="col-lg-6 col-md-3 col-sm-12">
		<img style="width: 95%;" src="{{ asset('images/contact.png') }}" alt="image enveloppe">
	</div>
	<div class="register col-lg-6 col-md-9 col-sm-12">
		<h2 class="title py-3 text-center">Contacter nous</h2>
		{{form_start(form)}}
		{{form_row(form.email)}}
		{{form_row(form.sujet)}}
		{{form_row(form.message)}}
		{{form_rest(form)}}
		{{form_end(form)}}

		</div>


	{% endblock %}
